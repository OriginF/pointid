#include "../def.hpp"
#include "../single_operation/negMatrix.hpp"

float K_win_calculator(int x0, int y0, int p, NegMatrix<int> Mn,map<int,int> point2index){
    float K_win = 0.0;
    for(int i=x0;i<x0+p;i++){
        for(int j=y0;j<y0+p;j++){

        }
    }
}